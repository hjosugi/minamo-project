<!-- i18n: language-switcher -->
[English](ENVIRONMENT.md) | [日本語](ENVIRONMENT.ja.md)

# Environment

- Date/time (UTC):
- Operator:
- Issue:
- Commit SHA:
- Worktree: `clean | dirty (attach diff)`
- OS and version:
- CPU:
- RAM:
- GPU and driver:
- Device model / firmware, if relevant:
- Browser and version:
- Node: `node --version`
- pnpm: `pnpm --version`
- Lockfile SHA-256:
- Rust/Cargo, if relevant:
- Application/tool versions:
- Network/TLS topology, with secrets and private addresses redacted:
- Licensed fixture hashes and redistribution/screenshot permissions:
- Consent record location, if media was captured:

Do not include device serials, tokens, private keys, raw private media, or full
local home-directory paths.

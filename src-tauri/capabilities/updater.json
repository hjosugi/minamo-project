{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "signed-updater",
  "description": "Signed update checks, installation, and restart from the main control window",
  "windows": ["main"],
  "permissions": [
    "updater:allow-check",
    "updater:allow-download-and-install",
    "process:allow-restart"
  ]
}
